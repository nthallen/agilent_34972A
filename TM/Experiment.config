Experiment=agilent
HomeDir=/home/agilent

# If running on a remote machine set FlightNode
FlightNode=spi4.arp.harvard.edu

# If not running on a remote machine and without
# a full server installation, then we need to
# define RUNFILE
# RUNFILE=interact

Extractions=agilentengext
RunType=ask
RunLog=saverun.log
